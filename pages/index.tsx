import type { NextPage } from "next";
import { Container, Layout } from "../components";

const Home: NextPage = () => {
	return (
		<Layout title="Home">
			<Container />
		</Layout>
	);
};

export default Home;
